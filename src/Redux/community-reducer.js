let initialState = {
    followCount:{
        subscribers:48,
        subscriptions:52
    }
}

const communityReducer = (state = initialState, action) => {

    return state;
}

export default communityReducer;